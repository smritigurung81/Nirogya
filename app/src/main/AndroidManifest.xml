<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nirogya">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Nirogya">

        <!-- ✅ LoginActivity is now launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Splash screen activity (no launcher) -->
        <activity android:name=".MainActivity" android:exported="false" />

        <!-- Other activities -->
        <activity android:name=".RegisterActivity" android:exported="false" />
        <activity android:name=".PatientDashboardActivity" android:exported="false" />
        <activity android:name=".DoctorDashboardActivity" android:exported="false" />
        <activity android:name=".LabTechnicianDashboardActivity" android:exported="false" />
        <activity android:name=".LabReportsViewerActivity" android:exported="false" />
        <activity android:name=".AppointmentManagerActivity" android:exported="false" />
    </application>

</manifest>

